import React from 'react';
import { TopHeader } from './styles';

const Header = () => {
    return (
        <header>
            <TopHeader>
                <span>asdf</span>
                <span>asdf</span>
            </TopHeader>
        </header>
    );
};

export default Header;
