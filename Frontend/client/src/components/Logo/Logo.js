import React from 'react';
import styled from 'styled-components';

const StyledText = styled.p`
  font-size: 3.6rem;
`;

const Logo = () => {
  return <StyledText>Logo</StyledText>;
};

export default Logo;
